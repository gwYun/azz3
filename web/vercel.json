{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "yarn install --immutable",
  "buildCommand": "echo 'no build step yet — Python serverless function in api/ is auto-detected via requirements.txt'",
  "outputDirectory": "."
}
